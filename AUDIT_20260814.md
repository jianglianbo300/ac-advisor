# ac_advisor 审计报告（2026-08-14，供 GPT 外部审查）

> 系统：上海闵行 70 平定频空调省电顾问 + 自动控制。每天 7:00 cron 跑一次，读天气+室内温湿度+空调插座实测功率，按状态机决策，自动执行到空调（红外），TTS 播报，微信推送。

---

## 一、系统全貌

```
Open-Meteo(室外温湿/降雨/趋势) ─┐
小米净化器4Lite(.120 室内温湿) ──┤→ ac_advisor.py(状态机决策)
空调伴侣mcn02(.43 实测功率/开关)─┘      │
                                       │ 状态机 → new_mode + burst_set
                                       │ ac_apply(): set_power/set_mode/set_tar_temp（红外，经空调伴侣）
                                       │ TTS: ac_tts.speak → 小米音箱
                                       └→ 微信推送(经 Hermes cron deliver)
```

- 唯一维护点：`D:\work\ac-advisor\ac_advisor.py`（630 行）
- cron 入口：`C:\Users\Administrator\.hermes\scripts\ac_advisor.py`（22 行薄包装器，runpy 跳真实脚本）
- 手动入口：`D:\work\ac-advisor\ac.py`（109 行，status/on/off/temp/mode/advice）
- 决策依赖真实状态：`ac_state.json`（mode/last_on_at/last_off_at/run_start）

## 二、当前配置/状态

- miio_config.json：净化器 ip=192.168.71.120 token=...；ac_partner=192.168.71.43 token=...；ac_control=true
- ac_state.json：mode="off"（last_off 2026-08-14T11:22）；run_start 仍是今天 10:30（残值）
- 阈值常量（头部）：制冷≥28；除湿开湿度>70（关 60，滞回）；除湿温度窗[26,28)；逃生门温度<24 关；MIN_RUN=40min / MIN_OFF=30min / MAX_RUN=180min
- 功率：铭牌 1076W / COP 3.25；实测（v7.0 起）制冷 26°C 运行 1037–1178W；待机 ~1–77W

## 三、已实测验证的事实（本次审计跑过）

1. `AirConditioningCompanionMcn02.status()`：is_on=bool、mode=OperationMode 枚举（**有 .value**，如 "cool"）、target_temperature=int(26)、fan_speed=FanSpeed 枚举、load_power=int
2. `send_command` 可用；`set_power/set_mode/set_tar_temp` 局域网直控生效；**`set_temperature` 会 ack timeout，必须用 `set_tar_temp`**；`set_swing` 本机不支持；`set_fan_level` 收 "auto_fan"/"small_fan"/"medium_fan"/"large_fan"
3. mcn02 构造器**不接受 timeout 参数**（传了 TypeError）
4. 红外命令实测链路：auto 开机→auto 关机（验证闭环）
5. 空调已关时 load_power≈1W（待机读数）

## 四、发现的问题（按严重度）

### 中（建议修）

**P1. ac_apply() 失败路径输出误导**
`main()` 里 `ctrl_act = ac_apply(...)`，非 None 打印"已自动执行"，`elif AC_CTRL is not None` 打印"已处目标状态，无需动作"。但 ac_apply 在**状态读取异常**（line 234）和**开机失败**（line 244）时也返回 None → 会被打印成"无需动作"，实际是控制失败/无法验证。且 `dehumid_alert` 分支不落在任何动作分支，同样被印成"无需动作"（实际是"建议切换"）。
→ 建议 ac_apply 返回三元（动作描述 / 无动作 / 失败），输出区分。

**P2. state.mode 与真实开关状态可能脱钩**
`save_state()` 在 `ac_apply()` 之前执行（line 529-530）。若自动控制失败，state.mode 记为 cooling 而空调实际没开；若用户手动关空调，state.mode 仍是 running。run_info 已优先用 AC_SOCKET（插座实测）显示真实状态，但**决策约束**（MIN_RUN/MAX_RUN）仍读 state.mode → 会出现"实际已关却建议防死锁切换"这类偏差。
→ 建议：插座可达时用 AC_SOCKET 对账/回写 state.mode；至少记录为已知偏差。

**P3. 湿度阈值文档漂移**
代码 `HUM_DEHUMID_ON = 70`（65→70 已改），但 AGENTS.md line 33 与头部 docstring E 仍写 65。文档滞后于代码。
→ 同步文档或标注生效值 70。

**P4. 分支 B 死代码**
分支 B 条件含 `26 <= signal < 28`，其内部 `if signal < TEMP_ABSOLUTE_FLOOR(24)`（line 452）**永远为 False，不可达**。v4.5 声称删死代码，此处漏删。
→ 删除。

### 低（清理项）

- L1. `ELECTRIC_PRICE`（line 50）定义后无引用，死常量
- L2. 头部 docstring 标题"v4.2"，输出打印"v8.0"，版本文案不一
- L3. `read_ac_power(timeout=4.0)` 的 timeout 参数从未使用（构造器不收）
- L4. 无并发保护：cron(7:00) 与手动 `ac.py advice` 同时跑会互踩 ac_state.json、双发控制命令
- L5. `tokens.json`（桌面 XiaomiTokenTool）明文存全部 13 台设备 token；`miio_config.json` 已 gitignore 但 token 仍明文——建议确认桌面权限/是否需加密
- L6. `ac.py cmd_temp` 无 try 保护：`st.mode.value` 若未来 miio 库改动会裸崩（当前实测枚举，安全但脆弱）
- L7. `read_indoor` 的 raw 兜底仅在温湿度**都**成功才返回；温成功湿失败会整体返回 None（浪费一次成功的温度读数）

### 设计张力（非 bug，需拍板）

- **T1. 自动控制 vs 用户手动自治**：cron 7:00 决策若为制冷会**自动开机**。用户昨晚手动"压一轮即关"关掉的空调，若次日晨湿度>70 且 26–28°C 会被重新自动打开。MIN_OFF 只防 30 分钟，不防隔夜。无"用户手动干预后 N 小时免自动控制"豁免。
- **T2. dry 模式 set_tar_temp 语义**：定频空调 dry 模式温度设定多数无效，ac_apply 仍会发 set_tar_temp（无害但可能无意义）。
- **T3. 估算系数全为拍脑袋**：COOL_DUTY 0.70 / DEHUMID_DUTY 0.60 / NIGHT_DUTY_26=(2×0.55+4×0.20)/6 / NIGHT_DUTY_24=(2×0.85+4×0.20)/6 / COP 3.25——耗电/电费数字都是估算量级，非实测。唯一实测点是功率 1037–1178W（1 轮样本）。
- **T4. 无降级缓存**：Open-Meteo 挂了脚本直接退出，无任何建议输出（不留上次缓存兜底）。
- **T5. TTS 失败静默**：speak 异常 except pass，音箱播报坏掉用户无感知。
- **T6. 无自动化测试**：决策表（含 4 个分支 × 锁约束 × alert）没有可运行的断言式自检；归档里有 test_*.py 但未纳入主脚本。

## 五、给 GPT 的核心待审问题

1. 状态机优先级（制冷>除湿>风扇>不开）与滞回/逃生门设计是否有逻辑漏洞或边界遗漏？
2. 分支 B0（24–26°C + 湿度>70 → 制冷 23°C 强制除湿一轮）与分支 B（26–28°C + 湿度>70 → 制冷 24°C）的衔接是否合理？湿度>70 但温度<24 落到"风扇/不开"，是否该有"低温高湿通风除湿"建议？
3. MIN_RUN/MIN_OFF/MAX_RUN 状态锁在"自动控制"下是否会与用户真实操作冲突？建议的豁免规则？
4. kwh_est 用实测功率回退铭牌，夜间估算用固定占空比系数——是否需要改用实测占空比/功率曲线标定？
5. P1/P2 的修复方案是否认可？有无更稳的 state 对账方式？
6. ac_apply 的 set_tar_temp 在 dry 模式下是否需要跳过？
