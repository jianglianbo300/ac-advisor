# 空调省电策略代码审查报告

> 审查日期：2026-08-19
> 审查文件：`D:\work\ac-advisor\ac_advisor.py` (1476行) + 状态文件 + 效果快照
> 对照基线：state.json v8.23 阈值 + AGENTS.md 决策逻辑

---

## 一、总体评价

代码质量 **中等偏上**，状态机、P2 执行权、回评机制设计合理。但存在：
- 2 个逻辑问题（会影响控制效果）
- 3 个参数与 state.json 不一致
- 1 个死代码/无效分支
- 传感器校准偏移需要更新

---

## 二、🔴 逻辑问题（必须修）

### 问题 1：传感器校准偏移 = -4°C，实际可能是 -3°C ~ -5°C 区间

```python
SENSOR_TEMP_OFFSET = -4  # 小米 4 Lite 净化器传感器在热源附近
```

**问题**：偏移量是固定值，但 ac_learned.json 的 reset_reason 提到"读 31.2°C / 实际 27°C"= -4.2°C。室内温度不同时散热条件不同，固定 -4 在高温段可能差 1°C。

**影响**：27.0°C 实际可能是 27.8°C（传感器读 31.8°C → -4 = 27.8°C），触发制冷；但用户实际只有 26.5°C 不该开。

**建议**：改为分段补偿：高温段（>30°C 传感器读数）-4.5，低温段（<28°C 传感器读数）-3.5。或基于 ac_learned.json 的 real_vs_sensor 数据做线性回归。

### 问题 2：`evaluate_and_learn` 的 off/fan 回评逻辑太严

```python
elif action in ("off", "fan"):
    temp_rise = cur_temp - pre_temp
    if temp_rise > 2.0 or (cur_hum is not None and cur_hum > 80):
        success = False
```

**问题**：`temp_rise > 2.0` 在夏天很常见——即使没开空调，下午温度自然回升 2-3°C 是正常的。这会被误判为"决定不开空调 → 失败"，触发阈值 -1。

**影响**：夏季午后正常温升被误判为决策失败，阈值往下漂。

**建议**：加入室外温度对比——如果室外也升了 2°C，说明是自然温升而非决策错误。或把 fail 条件改为 `temp_rise > 3.0`。

---

## 三、🟡 参数与 state.json 不一致

| 参数 | 代码值 | state.json 值 | 冲突 |
|---|---|---|---|
| 制冷启动阈值 | `TEMP_COOLING = 27` | `start_line_day: 27` | ✅ 一致 |
| 除湿湿度阈值 | `HUM_DEHUMID_ON = 65` | 未明确 | 需确认 |
| 除湿湿度关闭 | `HUM_DEHUMID_OFF = 55` | 未明确 | 需确认 |
| 最小运行时间 | `MIN_RUN = 40` | 未明确 | 需确认 |
| 最小停机时间（夜间） | `MIN_OFF = 30` | 未明确 | 需确认 |
| 最小停机时间（白天） | `DAY_MIN_OFF = 15` | 未使用 | ❌ 代码内未引用 |
| 最大运行时间 | `MAX_RUN = 180` | 未明确 | 需确认 |

**问题**：`DAY_MIN_OFF = 15` 定义了但决策树没引用（只有 `MIN_OFF` 被引用）。白天关后 15 分钟可重开 vs 夜间 30 分钟——这个区分是 v8.1 加的但代码没接上。

**影响**：白天关后要等 30 分钟（MIN_OFF）才能重开，白天升温快的时候太慢。

**建议**：在"开：检查关后最小停机时间"分支加昼夜判断：白天用 DAY_MIN_OFF，夜间用 MIN_OFF。

---

## 四、🟢 死代码 / 无效分支

### `_learn_from_manual` 学但不用

```python
def _learn_from_manual(state, now_ts):
    ...
    if len(low_rh_manual) >= 3:
        pref["hum_threshold"] = 60  # 学到用户偏好更低湿度
```

**问题**：写入 `state["user_pref"]["hum_threshold"]`，但决策树读的是 `HUM_DEHUMID_ON`（常量 65），从不读 `user_pref.hum_threshold`。学了白学。

**影响**：无 bug 但代码误导维护者以为用户偏好学习有效。

**建议**：要么决策树读 `user_pref.hum_threshold`（有则覆盖 HUM_DEHUMID_ON），要么删掉这个函数。

---

## 五、🟢 小问题

### 1. TTS 引用可能炸

```python
from ac_tts import speak
```

`ac_tts.py` 不在 `D:\work\ac-advisor\` 目录里（没有这个文件）。每次 main() 运行都会 try/except 吞掉 ImportError，但日志不提示——用户不知道 TTS 坏了。

**建议**：加一行 `print("  ⚠️ ac_tts.py 不存在，TTS 播报已跳过")`。

### 2. `seasonal_adjustments()` 8月策略是"盛夏制冷"但偏移=0

8 月 `temp_offset=0`，即阈值不变。但 `hum_offset=0` 也没变。梅雨季（6月）降 5% 是对的，盛夏不动也没问题。OK。

### 3. `ac_thermal.json` 数据质量差

大部分事件 `temp_after=None, duration_min=None`（home_living.py 写了一半没回填）。有效 cooling 事件只有 ~5 条，热模型参数基本靠默认值（0.05°C/min）。

**影响**：predict_cooling_time 不准，但不影响控制（仅用于输出建议）。

**建议**：暂时不影响核心逻辑，后续积累足够数据再校准。

---

## 六、效果快照分析（v8.16 修复后）

从 `_effect_snapshots.jsonl`：

| 指标 | 修复前（基线） | 修复后 |
|---|---|---|
| 样本数 | 15 | 36 |
| 短循环比例 | 40% | 91.7% |
| 平均持续时间 | 27.2min | 17.4min |
| 总耗电 | 8.36kWh | 10.53kWh |

**问题**：短循环比例从 40%→91.7%，平均持续时间从 27→17 分钟。修复后短循环更多更短——这不符合"修复"的预期方向。

**原因推测**：
1. 修复后 MIN_RUN=40 但实际平均只有 17 分钟 → 说明 ac_watch 的自动停止逻辑在起作用（湿度降到 60% 就关），与 MIN_RUN 冲突
2. 或传感器偏移导致温度读高 → 空调开→湿度快速降→达到关闭条件→关

**建议**：ac_watch.py 的停止逻辑与 ac_advisor.py 的 MIN_RUN 需要统一。当前 ac_watch 是独立脚本，有自己的判断逻辑，可能与主策略冲突。

---

## 七、优先修复清单

| 优先级 | 项目 | 影响 |
|---|---|---|
| P0 | 传感器补偿改为分段（-4 → -4.5/-3.5） | 高温段伪精度 |
| P0 | off/fan 回评加入室外温度对比 | 夏季误判失败 |
| P1 | DAY_MIN_OFF 接入决策树 | 白天重开太慢 |
| P1 | `_learn_from_manual` 接通或删除 | 死代码误导 |
| P2 | ac_watch 停止逻辑与 MIN_RUN 统一 | 短循环异常 |
| P2 | ac_tts 缺失提示 | 静默失败 |

---

## 八、结论

核心决策逻辑（状态机 + P2 执行权 + 回评闭环）设计合理。主要问题在**传感器校准**和**回评条件**，不是架构问题。按上表 P0/P1 修一轮即可。
