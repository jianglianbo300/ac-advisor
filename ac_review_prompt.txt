审查 D:\work\ac-advisor\ac_advisor.py（约1482行），具体检查以下问题：

1. **状态机校验重复**：1222-1227 行和 1228-1234 行重复做了状态机校验，后者完全覆盖前者。确认是否为死代码？

2. **手动关锚点硬编码**：1060 行 `if 0 <= mins < 15` 硬编码了 15 分钟，但 MIN_OFF=15、DAY_MIN_OFF=10。确认这里的 15 分钟是否应该改成动态引用？

3. **低湿过冷收手边界**：1239-1241 行 `hum_sig < 45` 且 `signal <= target_temp + 1`。确认 45% 这个阈值是否合理？会不会导致湿度 46% 时空调频繁启停？

4. **v12.0 决策逻辑完整性**：1168-1220 行的白天/夜间分支。确认：
   - 夜间湿度 >60% → 除湿，是否应该检查温度？
   - 白天湿度 >65% → 除湿，温度 >=27°C → 制冷，优先级是否正确？
   - 26-27°C 区间（`signal >= 26` 但 `< 27`）走风扇，是否应该考虑湿度？

5. **最小运行/停机约束**：1248-1262 行。确认：
   - `since_off < MIN_OFF` 时 `new_mode = "fan_locked"`，但 `MIN_RUN` 检查在关分支里是否正确？
   - 有没有可能 `since_on < MIN_RUN` 但 `new_mode = "cooling"` 导致频繁启停？

给出每个问题的行号、代码片段、判断（正确/有风险/错误）、建议。
