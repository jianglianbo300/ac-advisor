# 空调策略 GPT-6 Astra 外审报告（2026-09-05）

> 审计方: gpt-6-astra (explabs, $0, 两刀分送: pass1=ac_watch.py 控制核心 16992 prompt tokens / pass2=ac_advisor.py 学习闭环 10007 tokens)
> 触发: 用户指令。输入为决策路径切片（decide/main/reconcile/evaluate_and_learn/compute_optimal_schedule/_learn_from_manual），非全量代码。
> Hermes 复核: pass2#1（调度四元组/三元组解包错位）、pass1#3（running 重建）、pass2#7（low_rh_manual 无 mode 过滤）三条已逐一对照源码实证属实，见文末。

## Pass 1 — ac_watch.py 控制核心（8 条）

以下仅依据贴出的 **`ac_watch.py` 代码**，不推定未展示的 `ac_advisor.py` 内部行为。确认 **8 项缺陷**，按严重度排序；没有足够证据将任何一项定为 P0。

## 1. P1：有天气兜底时，室内传感器断连超时保护失效

**位置**：`main()` 的天气兜底、`_sensor_off_since` 检查及后续周期记录。

**代码证据**
- 只要室温、湿度任一缺失，就用室外温度覆盖 `temp`，并设置 `hum = 50`。
- `SENSOR_TIMEOUT_ESCALATE` 检查位于后面的 `if temp is None or hum is None` 内；兜底成功后，该分支不可达。
- `wx_fallback_used` 分支仅建立断连时间，不检查是否已经超时。

**触发条件**：空调正在运行，室内传感器持续离线，但天气 API 或缓存持续可用。仅湿度掉线、室温仍有效，也会进入这条路径。

**后果**
- 无论断连多久，都不会执行这项“断连超时保守关机”保护；其他停机条件可能仍生效，但不能替代它。
- 运行决策使用室外温度和虚构 RH=50，而非室内实测。
- 室外温度仍进入 `temp_history`；若随后关机，`close_cycle()` 会把这些值写成周期结束温湿度，并传给 `record_thermal_event()`，调用处没有数据来源过滤。

**修法**：保留独立的“室内数据有效性”标志，在兜底前维护断连计时，并无条件检查超时。天气数据不得冒充室内观测进入控制历史、周期效果样本或热模型事件；单项掉线也不应覆盖另一项有效读数。

#### 2. P1：`fan_only` 分支提前返回，绕过过冷保护和运行时长保护

**位置**：`decide()`，`if compressor == "fan_only":`。

**代码证据**
- 该分支位于 `WATCH_MAX_RUN`、夜间绝对温度下限和通用绝对温度下限检查之前。
- 分支内部所有路径都会返回，包括末尾的无动作返回。
- 湿度高且停机时间足够时，优先返回降温重启指令，没有室温下限检查。

**触发条件**：`running=True`、功率被分类为 `fan_only`，且本周期已有压缩机运行时间，从而不触发最前面的零运行止损。

**后果**：即使室温已经低于绝对下限，也可能返回“降温重启压缩机”或保持运行，而不是执行注释所称的“无条件关机”。累计运行时长保护也无法覆盖该分支。

**修法**：把绝对温度下限等硬安全检查移到风扇恢复逻辑之前；明确最大运行限制是否覆盖风扇阶段，不要靠分支提前返回隐式豁免。

## 3. P1：主循环重新相信 `socket=is_on`，可以架空幻象对账结果

**位置**：`main()`，计算 `running` 的代码。

**代码证据**
```python
running = socket == "on" or state.get("mode") in (
    "cooling", "dehumid", "dehumid_alert",
)
```
这里的 `socket` 是调用 `reconcile_state()` **之前**取得的 `A.AC_SOCKET` 快照。上述判断既不使用功率，也不使用 kWh 证据。

**触发条件**：伴侣幻象报告 `on`，实际待机，且对账后的 `state["mode"]` 为 `off`。

**后果**
- 对账即使已经正确认定关机，主循环仍认定运行。
- DP 和预除湿计算因 `not running` 为假而跳过；`decide()` 也不会进入正常开机路径。
- 例如 `run_start=None`、累计压缩机分钟为零时：零运行止损因缺少 `since_on` 不触发，随后最小运行门返回无动作。即使室内闷热，也可能一直不启动。
- “兜底温度不开新周期”的保护同样依赖这个错误的 `running`，不再可靠地表示物理运行状态。

**修法**：由对账层输出统一的有效运行状态及证据等级，后续控制只使用该结果。不要在对账之后再用原始 `is_on OR state` 重建运行状态。

## 4. P1：通风门仅凭露点差，就能无限拦截必要制冷

**位置**：`vent_gate_decision()` 及 `main()` 执行开机前的通风拦截。

**代码证据**
```python
return dp_out <= dp_in - VENT_GATE_DP_DIFF
```
函数虽然接收 `hour、hum、temp、rain`，实际均未使用。调用处对所有从关机进入制冷的动作统一拦截，没有区分“降温需求”与“除湿需求”。

**触发条件**：室内温度已满足制冷启动条件，但室外空气更干、露点差满足门限。例如室外炎热干燥，室内闷热。

**后果**
- 露点低只说明空气含水量低，不代表能够给室内降温；热而干的室外空气也会阻止制冷。
- 代码没有开窗执行、开窗确认或拦截超时。门限持续成立时，可以每轮拦截开机，却没有证据表明“免费除湿”真的发生。

**修法**：通风优选至少同时检查室外温度或焓值、降雨和通风可用性；高温制冷需求应有硬豁免。无开窗确认时不能无限期以通风替代制冷，应设置短暂建议窗口和退出条件。

## 5. P2：调度 override 可以绕过每小时启动次数上限

**位置**：`decide()` 的启动限次门，以及 `main()` 中 `if new_mode is None` 的调度覆盖。

**代码证据**
- 达到 `_starts_1h` 上限时，`decide()` 返回 `(None, None, None)`。
- 主循环无法区分这是“安全门禁止启动”还是“暂无舒适性需求”。
- override 执行前只重新检查 `MIN_OFF`，随后直接设置：
```python
new_mode, target, reason = "cooling", _schedule_target, _schedule_reason
```

**触发条件**：正常决策因每小时启动次数达到上限而拒绝启动，停机间隔已经满足，且 DP 或预除湿 override 为真。

**后果**：调度重新发出启动请求，每小时限次门实际约束不了该路径。最短停机时间仍受保护，但不能因此认为启动次数上限也受保护。

**修法**：把最短停机时间、每小时限次等统一为所有开机入口都必须通过的最终许可门；或者让 `decide()` 返回明确的拒绝类型，禁止调度覆盖硬限制。

## 6. P2：开机尚未执行就记决策，通风拦截和 dry-run 也进入学习调用

**位置**：`main()`，`log_decision()` 与 dry、通风门及 apply 的顺序。

**代码证据**
```python
log_decision(state, new_mode, temp, hum, now_ts, reason=reason)
```
发生在以下步骤之前：
1. `if dry`；
2. 通风门拦截；
3. `A.apply_and_commit()` 及其执行结果判断。

dry 和通风拦截路径随后都调用 `evaluate()`；通风拦截路径还保存状态。

**触发条件**：产生 `cooling` 决策，但被通风门拦截、处于 dry-run，或者实际控制失败。

**后果**
- 在调用接口层，未执行动作已经以普通 `cooling/off` 决策形式提交；调用处没有提供 `executed=False` 或执行结果。
- 本文件的启动次数统计读取 `decision_log`，按 `action` 转换计数，不检查执行结果。因此只要这些记录进入该日志，就会把未执行的开机计划计入启动次数。
- 同一批未执行决策立即进入学习调用。**未展示学习函数，不能进一步断言它必然如何调整权重；但这里确实缺少区分“计划”和“已执行动作”的信息。**

**修法**：分开记录计划日志与执行日志；以 apply 成功结果生成实际动作事件，并显式记录失败、拦截和 dry-run。启动限次及行为反馈只消费实际执行事件。

## 7. P2：H2 接管计算了目标温度，却没有真正继承

**位置**：`main()`，

## Pass 2 — ac_advisor.py 学习闭环（7 条）

以下仅依据所贴代码审计。未提供 `main()/decide()/apply` 和 H2 实现，因此不推断这些缺陷是否会被外围捕获或兜底。共发现 **7 条**，按严重度排序。

### 1. 【高】预冷窗口函数无法消费 DP 生成的调度结果

- **位置**：`compute_optimal_schedule()` → `find_pre_cool_window()`。
- **代码证据**：生产端写入四元组：
  ```python
  schedule.append((h, action, cost, t_current))
  ```
  消费端却按三元组解包：
  ```python
  for i, (h, action, cost) in enumerate(schedule):
  ```
- **触发条件**：把任何非空的 DP 返回结果传给 `find_pre_cool_window()`。
- **后果**：立即抛出 `ValueError: too many values to unpack`，预冷窗口无法生成；是否中断整个 tick 取决于未展示的调用方。
- **修法**：统一调度记录格式，最好使用具名字段。**仅修改解包数量还不够**：生产端的 `h` 是相对偏移，消费端却用 `6 <= h <= 21` 当绝对小时判断，且 `current_hour` 完全未使用。应传递带时区的实际时刻，并据此判断峰谷及跨日窗口。

### 2. 【高】手动学习仍存在绕过 kWh 验证的入口，幻象可污染“手动关机”偏好

- **位置**：`reconcile_state()` 的关翻转分支及延迟学习到期分支。
- **代码证据**：
  - 当上一拍 `_prev_power > 50`、当前功率 ≤50W 时，可以直接打 `manual_off_at` 并调用 `_learn_from_manual()`；没有检查 kWh，也不经过震荡检测——震荡检测仅适用于当前 `load_power is None`。
  - 延迟开机学习也明确允许无电量证据入账：
    ```python
    if _k0 is None or _k1 is None or (_k1 - _k0) >= 0.005:
        _learn_from_manual(...)
    ```
- **触发条件**：例如，无近期震荡标记时，幻象 `on + 1076W` 先建立运行态；下一拍变成 `off + 1W`，但电量始终不增加。关翻转仍满足直接学习条件。另一入口是延迟核验任一端电量为 `None`。
- **后果**：开机假样本即使被零电量拦住，配对的**关机假样本仍会入账**；反复发生可将舒适权重偏向“用户倾向关机”。电表不可用时，开机假样本也能绕过验证。
- **修法**：对手动开、关学习统一要求可核验的事件证据。关机事件至少核验被中断的运行阶段确有电量消耗；电量缺失应暂缓或丢弃学习，而不是视为验证通过。控制层是否暂时尊重疑似手动操作，应与学习资格分开。

### 3. 【高】超预算增量抵消健康护栏，偏移仍能锁在高位

- **位置**：`evaluate_and_learn()`，预算调整之后执行“偏移健康护栏”。
- **触发条件**：持续超预算、没有其他有效回评降低偏移；白天室温 ≥`TEMP_COOLING`，距 `last_off_at` 超过20分钟。例如当前偏移为 `2.5`。
- **代码证据**：同一次调用先因超预算加 `0.5`，再因护栏减 `0.5`：
  ```text
  2.5 → 3.0 → 2.5
  ```
  后续调用重复上述过程。即使起点为 `3.0`，也只是首轮降到 `2.5`，随后锁住。
- **后果**：注释承诺的“高温下自愈”无法持续回落；若消费方按注释将偏移加到启动线，启动线可长期维持默认值以上 `2.5°C`。
- **修法**：把预算和舒适护栏合成一次有优先级的更新：护栏触发时禁止预算上调，并保证净回落。预算调整还应按明确的评估窗口去重，避免相同超预算事实每次调用重复加码。

### 4. 【中】延迟验证使用十分钟后的模式和湿度，开机事件会被学成关机事件

- **位置**：`reconcile_state()` 的 `_pending_manual_on_learn` → `_learn_from_manual()`。
- **代码证据**：待验证记录只保存 `ts`、`kwh`；到期调用传入的是当前 `state`。学习函数取：
  ```python
  rh_hist[-1][1]
  state.get("mode")
  ```
- **触发条件**：用户真实开机，运行几分钟后关机；十分钟核验时，电量增量已超过 `0.005kWh`，但当前模式为 `off`。
- **后果**：原本的“手动开机”记录被写成 `mode="off"`，湿度也取错时点。如果实际关机已入账，就会得到两条关机样本、丢掉开机样本，系统性偏向降低舒适权重。
- **修法**：创建待验证事件时保存不可变快照，例如 `{ts, action, rh, temp, kwh_start}`；核验只决定是否接纳该快照，不能重新从当前状态推断原始动作与环境。

### 5. 【中】普通决策回评仍把单帧功率当成成功证明

- **位置**：`evaluate_and_learn()` 的 `cooling/dehumid` 成功判定。
- **代码证据**：
  ```python
  comp_running = (entry.get("power_at_decision") or 0) > 300
  if comp_running:
      success = True
  ```
  一旦决策时功率超过300W，就跳过温湿度效果判断，也没有 kWh 验证。
- **触发条件**：`decision_log` 中记录了幻象高功率，30～120分钟后被回评；即使没有电量增加、温湿度完全未改善，也会成功。
- **后果**：假成功样本使正偏移执行 `-1` 回落。手动学习新增的电量门控并未保护这条独立学习路径。即使功率真实，**决策前正在运行**也不能证明随后决策成功。
- **修法**：记录并核验决策后的运行区间、电量增量及效果；单帧功率只能作为辅助信息。证据不足时标记“不可评价”，不要强制归为成功或失败。

### 6. 【中】带时区天气时间会使预除湿预测静默失效

- **位置**：`predict_dehumidify_need()`。
- **代码证据**：
  ```python
  t_dt = datetime.fromisoformat(t)
  hours_ahead = (t_dt - datetime.now()).total_seconds() / 3600
  ```
  `datetime.now()` 是无时区对象；解析带 `+08:00` 等偏移的 ISO 时间后，`t_dt` 是有时区对象。
- **触发条件**：天气接口返回带偏移的合法 ISO 时间。
- **后果**：每条时间相减都抛 `TypeError`，又被 `except: continue` 吞掉，最终固定返回“不需要预除湿”，没有错误提示。
- **修法**：统一转换为有时区时间，使用同一时区的 `now`；无偏移时间应按天气接口约定显式解释。另外应先过滤有效湿度再聚合：当前选中区间湿度全部为 `None` 时，`max()` 会对空序列抛异常，而不是正常返回数据不足。

### 7. 【中】湿度偏好学习不区分开关动作，手动关机也会降低除湿阈值

- **位置**：`_learn_from_manual()` 的 `low_rh_manual`。
- **代码证据**：
  ```python
  low_r### 7. 【中】湿度偏好学习不区分开关动作，手动关机也会降低除湿阈值

- **位置**：`_learn_from_manual()` 的 `low_rh_manual` 筛选。
- **代码证据**：
  ```python
  low_rh_manual = [m for m in manual_log if 60 <= m.get("rh", 0) < 65]
  if len(low_rh_manual) >= 3:
      pref["hum_threshold"] = 60
  ```
  筛选没有限制 `mode`，而同一日志已同时接收手动开机和关机事件。
- **触发条件**：最近20条手动记录中，有至少3条发生在 RH 60%～65%，即使这3条全部是用户手动关机。
- **后果**：用户明确停止运行的行为，被解释为“希望在较低湿度就除湿”，写入 `hum_threshold=60`。这是偏好学习方向反转；是否进一步触发自动除湿，取决于未展示的阈值消费逻辑。
- **修法**：至少只让已验证的手动开机事件参与降低湿度阈值；更稳妥的是区分制冷与除湿意图，不能仅凭开机时湿度判断用户需要除湿。关机事件应独立评价，不应计入降低阈值的支持样本。

以上补完第7条；不再新增缺乏充分证据的项目。

## Hermes 抽查实证（2026-09-05）

1. **pass2#1 调度格式错位 — 属实**：ac_advisor.py:188 `schedule.append((h, action, cost, t_current))` 四元组，:195 `for i, (h, action, cost) in enumerate(schedule)` 三元组解包。任何非空 schedule 传入即 ValueError。预冷窗口功能当前整体不可用。
2. **pass1#3 running 重建 — 属实**：ac_watch.py:1217 `running = socket == "on" or state.get("mode") in (...)`，socket 为 reconcile_state 之前的原始快照，无功率/kWh 佐证，与 v8.48 确立的"kWh > 功率 > is_on"事实等级相悖。
3. **pass2#7 湿度偏好方向反转 — 属实**：ac_advisor.py:1355 `low_rh_manual = [m for m in manual_log if 60 <= m.get("rh", 0) < 65]` 无 mode 过滤，手动关机样本同样压低 hum_threshold。

## 处置建议（待用户拍板）

- **P0 候选**：pass2#1（预冷窗口函数一调即崩——但需先确认调用点是否实际可达/被 try 吞掉，若从未触发则降级为潜伏死代码）；pass2#2（幻象关机样本绕过 kWh 验证——v8.48 门控的真缺口）。
- **P1**：pass1#3（running 重建架空对账）、pass2#4（延迟学习取当前 state 快照错时点）、pass2#3（预算+护栏相互抵消锁死偏移）。
- 修复应走既有分支重放纪律：先补重放用例（含幻象 on→off 翻转序列、带时区天气 ISO 时间、非空 schedule 调用 find_pre_cool_window），再动代码。
- 与 NDX 外审同类教训：Astra 指出的"测试集复用/单帧功率当成功证明"（pass2#5）是方法论级发现，值得进 Lessons。

## §八 v8.50 实施记录（2026-09-05 用户拍板"听他的弄"）

- **实施**：15/15 项全部落地，commit 3b72a48（v8.49→v8.50，VERSION 单一戳）
- **纪律**：先写 _replay_v850.py 24 用例见红（6/24）→ 修码 → 全绿 24/24；v848 重放 25/25 回归；快测 12 文件全 OK；selftest + py_compile 绿
- **pass2 七项全落地**（find_pre_cool_window 解包/护栏优先/锚点快照/kWh 回评佐证/时区修复/low_rh 过滤/幻象关翻转 kWh 佐证）
- **pass1 八项全落地**（兜底超时关机/逃生门前移/running 对账出口/vent 高温豁免/override 限次/executed 标记/H2 target 继承/kWh 账本前移 + dry 保护）
- **测试中修正的 2 处测试构造错误**（非代码问题）：V7 断言 len==1 改为存在 mode=off 条目（开+关样本各归各位）；V10 _daily_kwh=0 误触预算分支改中性 8.0
- **遗留**：kWh 跨午夜/峰谷边界精确按积分区间拆分未做（2min tick 粒度误差 ≤1 tick，已注释）；H2 幻象高功率 kWh 否定证据未加（等实战证据，防过度拟合）

## §九 v8.50b→v8.50e 验收闭环记录（2026-09-05）

- **验收迭代**：v8.50(15项初修) → 验收一 4✅/10⚠/1❌ → v8.50b → 验收二 7✅/8⚠/0❌ → v8.50c → 验收三 → v8.50d → 验收四 8✅/7⚠/0❌ → v8.50e → 验收五 4✅/2⚠/1❌(本批7项) → 再修
- **验收方指出的确定性缺陷（全部修复）**：
  1. #8 未执行条目不更新 _prev_act（漏计/多计双缺陷，v8.50b）
  2. #12 显式快照 rh 依赖当前 rh_history（v8.50b）
  3. #1 保守关机失败仍清计时（v8.50b）
  4. #3 高功率路径加 H2 门控（v8.50b）
  5. #8 窗口初态取最后一条已执行记录（v8.50c→d 修正）
  6. #12 缺 rh 只跳学习不退出 reconcile（v8.50c 回归，v8.50d 修）
  7. #2/#4 电量缺失不喂样本（v8.50d）
  8. **#2 关翻转直接学习入口电量准入（v8.50e，验收五确认的最后一处真残留）**
- **验收方确认闭环的项**：pass1#1/#2/#4/#5/#7、pass2#1/#3/#4/#5/#6/#7、#8 消费端过滤
- **教训**：①"验收脚本切片 bug"会制造大量假 ⚠️——给外部模型贴代码必须按函数边界完整提取，不能按固定行数截断；②测试循环 `for f; do ... || echo FAIL; done && echo OK` 会吞 FAIL（ALL_TESTS_OK 无条件打印）——验收前必须修正此类测试框架缺陷；③线上状态断言（如"偏移==0"）与部署后动态行为耦合，应断言合法域而非静态值
- **遗留**：kWh 跨午夜精确拆分（P3，可观察）；回评电量归因跨周期混入（P3，可观察）；旧日志缺 executed 兼容期（P3，观察）

## §十 最终签收（2026-09-05 22:00）

- **Astra 第七轮验收：15 项全部闭环**（pass2#2/pass1#6/pass1#3 三未决项全部 ✅ 签收）
- 最终版本 v8.50f (d6cc97c)，提交链：v8.50(3b72a48)→b(63db23f)→c(32bb040)→d(9e6e917)→e(17b1741)→f(d6cc97c)
- 验证栈：_replay_v850.py 44/44（先红后绿，覆盖每项修复的缺陷复现+正例对照）+ v848 25/25 + 12 快测 + selftest + py_compile
- **验收历程 7 轮**：初修 15 项 → 4✅/10⚠/1❌ → b 修 4 项 → 7✅/8⚠/0❌ → c 修 3 项 → d 修 3 项 → 8✅/7⚠/0❌ → e 修 1 项 → 5 项内 4✅/2⚠/1❌ → f 修 1 项 → 7 项内全 ✅
- **方法论收获（已记 Lessons）**：①给外部模型贴代码必须按函数边界完整提取，固定行数切片会产生假"缺正文"；②测试循环 FAIL 吞没 bug（ALL_TESTS_OK 无条件打印）；③线上状态断言应与动态行为解耦（断言合法域而非静态值）；④验收-修复迭代中，修复后的代码必须重新提取快照再送审（v8.50e/f 两次因快照过期被拒签/暂缓）
